const PROJECT_DIR = `${process.cwd()}`;
const MOCK_DATA_DIR = `${PROJECT_DIR}/api-mock/data`;
const API_BASE_URL = 'http://localhost:8080';

module.exports = {
    PROJECT_DIR,
    MOCK_DATA_DIR,
    API_BASE_URL,
};
