import * as React from 'react';

interface Props {
    putPropsHere?: string
}

const RenameMe: React.FC<Props> = (props: Props) => {
    return (
        <div>
            Put stuff here
        </div>
    )
};

export default RenameMe;
